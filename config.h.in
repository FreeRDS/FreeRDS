#ifndef __CONFIG_H
#define __CONFIG_H

#define FREERDS_VERSION_MAJOR ${FREERDS_VERSION_MAJOR}
#define FREERDS_VERSION_MINOR ${FREERDS_VERSION_MINOR}
#define FREERDS_VERSION_REVISION ${FREERDS_VERSION_REVISION}
#define FREERDS_VERSION_SUFFIX "${FREERDS_VERSION_SUFFIX}"
#define FREERDS_VERSION "${FREERDS_VERSION}"
#define FREERDS_VERSION_FULL "${FREERDS_VERSION_FULL}"
#define GIT_REVISION "${GIT_REVISION}"

#define FREERDS_CFG_PATH "${FREERDS_CFG_PATH}"
#define FREERDS_SBIN_PATH "${FREERDS_SBIN_PATH}"
#define FREERDS_SHARE_PATH "${FREERDS_SHARE_PATH}"
#define FREERDS_VAR_PATH "${FREERDS_VAR_PATH}"
#define FREERDS_PID_PATH "${FREERDS_PID_PATH}"
#define FREERDS_LOG_PATH "${FREERDS_LOG_PATH}"
#define FREERDS_LIB_PATH "${FREERDS_LIB_PATH}"

#define FREERDS_INSTALL_BINDIR "${CMAKE_INSTALL_BINDIR}"
#define FREERDS_INSTALL_SBINDIR "${CMAKE_INSTALL_SBINDIR}"
#define FREERDS_INSTALL_LIBDIR "${CMAKE_INSTALL_LIBDIR}"
#define FREERDS_INSTALL_DATAROOTDIR "${CMAKE_INSTALL_DATAROOTDIR}"
#define FREERDS_INSTALL_LOCALSTATEDIR "${CMAKE_INSTALL_LOCALSTATEDIR}"
#define FREERDS_INSTALL_SYSCONFDIR "${CMAKE_INSTALL_SYSCONFDIR}"

/* Include files */
#cmakedefine HAVE_FCNTL_H
#cmakedefine HAVE_UNISTD_H
#cmakedefine HAVE_STDINT_H
#cmakedefine HAVE_INTTYPES_H
#cmakedefine HAVE_SYS_MODEM_H
#cmakedefine HAVE_SYS_FILIO_H
#cmakedefine HAVE_SYS_STRTIO_H
#cmakedefine HAVE_EVENTFD_H
#cmakedefine HAVE_TM_GMTOFF

#endif

